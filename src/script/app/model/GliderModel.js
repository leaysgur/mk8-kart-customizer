define([
    'common/model/BaseModel'
], (
    BaseModel
) => {
    class GliderModel extends BaseModel {
        constructor() {
            super();
        }
    }

    return (new GliderModel());
});
